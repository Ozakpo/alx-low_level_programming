#include <stdio.h>

/**
 * main - prints a given phrase
 * Return: 0 if exited properly, or return a not zero value
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle")
	return (0);
}
